commit-msg:
  jobs:
    - name: commitlint
      run: npx commitlint --edit {1}

pre-commit:
  parallel: true
  jobs:
    - name: biome
      run: pnpm biome check --staged --no-errors-on-unmatched
    - name: typecheck
      run: pnpm lint:types
    - name: knip
      run: pnpm lint:knip
